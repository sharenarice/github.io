# Core vision & UI
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
Pillow>=10.0.0

# Desktop notifications (optional but recommended)
plyer>=2.1.0

# Fast person detection (optional — app falls back to HOG if absent)
# ultralytics>=8.0.0
